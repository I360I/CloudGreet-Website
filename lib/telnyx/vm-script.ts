/**
 * Default voicemail-drop script, spoken via Telnyx TTS when the rep
 * clicks "Drop VM" and hasn't saved a personal script yet
 * (custom_users.vm_drop_script). Shared by the voicemail-drop route and
 * the settings UI (shown as the textarea placeholder).
 */
export const DEFAULT_VM_SCRIPT =
  "Hi, this is CloudGreet - sorry we missed you. We help home service " +
  "businesses stop missing calls with an AI receptionist that answers, " +
  "books, and follows up automatically. Give us a call back or text this " +
  "number back and we'll get you set up. Thanks, have a great day."
